// True or False ***********************************************************************************

// task#1

// if ("0"){
//   alert('heellllo'); // heellllo
// }

/*
// task #2

let correct = "ECMAScript";
let answer = prompt("Whats the original name of JavaScript?");
if(answer==correct){
    alertt("Correct!")
}
else{
    alert("you don't know?,The original name is EMCAScript!")
}
// task #3

let score = 75;

let grade = score >= 90 ? "A" : score >= 80  ? "B"  : score >= 70 ? "C"  : score >= 60 ? "D"  : "F";
console.log("הציון הוא:", grade);

// task #4

let number = prompt("הכנס מספר:");

number = Number(number); // המרה ממחרוזת למספר

if (number > 0) {
  alert("הערך גדול מאפס");
} else if (number < 0) {
  alert("הערך קטן מאפס");
} else {
  alert("הערך הוא אפס");
}

//task #5

let result = a + b < 4 ? "Not enough" : "A lot";

// task #6

let message =
  login == "Employee" || login == "Director" ? "Hello"  : login == ""  ? "No login"  : "";

  // task# 7

console.log(false || 'sdf');        // 'sdf'
console.log(false && '');           // false
console.log(true || '');            // true
console.log(true && '');            // ''
console.log(undefined || null);     // null
console.log(undefined || 0 || null); // null
console.log(undefined && 0 && null); // undefined
console.log(1 || 0);                // 1
console.log(null || 1);             // 1
console.log(null || 0 || 1);        // 1

*/
